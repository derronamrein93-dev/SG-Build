name: core_theme
description: >-
  ThemePack model, slot vocabulary, token catalogs and the resolver. Pure Dart —
  it deals in asset paths, never in loaded images.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  core_foundation: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
