name: core_foundation
description: Primitives every other package depends on. Depends on nothing itself.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
