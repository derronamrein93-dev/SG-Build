name: playhub_tools
description: >-
  Build-time enforcement and content tooling. Everything here runs in CI on
  Linux, with no simulator and no license.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
