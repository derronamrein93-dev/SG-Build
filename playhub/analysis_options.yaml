include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  # Map form, not list form: turning an inherited rule *off* requires it.
  rules:
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    collection_methods_unrelated_type: true
    comment_references: true
    implicit_reopen: true
    only_throw_errors: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_single_quotes: true
    sort_pub_dependencies: true
    test_types_in_equals: true
    throw_in_finally: true
    unawaited_futures: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    use_super_parameters: true

    # Off deliberately: it fires on `: _field = namedParam`, but the suggested
    # `this._field` makes the parameter private and so unusable by callers
    # outside the library. The lint does not model named parameters.
    prefer_initializing_formals: false
